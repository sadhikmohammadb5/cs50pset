#include<stdio.h>
#include<cs50.h>
#include<string.h>
#include<ctype.h>
 int main(void)
 {
    string s=get_string("before: ");
    printf("after:");

    for(int i=0;(n=strlen) i<n;i++)
    {
        if(s[i]>='a' && s[i]<= 'z' )
        {
            printf("%c",toupper s[i]);
        }
        else
        printf("%c,s[i]);

    }
 }
